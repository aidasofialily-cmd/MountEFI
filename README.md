# 💾 MountEFI

A lightweight, terminal-based utility to quickly mount the EFI partition on macOS.

## 🚀 Usage

1. **Clone the repo:**
   ```bash
   git clone https://github.com/aidasofialily-cmd/MountEFI
   cd MountEFI
